# 主线整合与候选状态

GitHub 默认分支为 `codex/runner-baseline`。2026-09-08 整合前，它停在 `4722b0b`；`codex/moe-composition` 的 `9a60fbd` 和 `codex/upstream-adoption` 的 `0309521` 是其连续后继，共 33 个提交，没有分叉冲突。本次保留这段完整历史，以快进方式纳入默认分支。

## 本次纳入内容

| 内容 | 主线中的使用状态 |
| --- | --- |
| HTTP/SSE 适配器、UTF-8 流式解码、有界输出与日志、断连清理 | `serve-gpu` 可用；有限纯文本协议，尚未完成生产验收 |
| MTP 完整请求成本、调度延迟、GPU 命令与状态诊断 | 可用；prefill 和 decode 分开计量，诊断计时不替代普通吞吐 |
| 实验进程组清理与参考服务恢复 | 公共脚本可用，只管理已登记的自有进程 |
| expert32 gate/up + grouped down | 普通生成的显式 prefill 选项；新增从本机插件构建清单生成配置的命令，无需历史本地结果文件 |
| 融合归约、QSA 融合、async8、其他验证 kernel | 保持各自原来的可选或算子探针范围；没有稳定收益的候选不启用为默认 |
| 完整混合状态前缀复用、SSD 状态缓存、完整工具协议 | 仍为后续工作，代码整合不代表已经实现 |

chunk416、eval4、SSD nextChunk、单读取 worker、符合条件的 causal attention 融合，以及 typed PD / 合作调度，在整合前已经存在于默认分支。本次不把这些重复计为新增收益。默认生成继续采用 AR 与 reference 累加；MTP 发布条件保持生效。

首页现在说明当前 Swift/MLX runner；早期 Core ML/ANE 内容完整迁至[历史文档](COREML_ANE_HISTORY.md)。[expert32 配置命令](../README.md#可选-expert32--grouped-down)需要本机兼容的固定 MLX 源码、构建树和动态库；仓库不提供模型或预编译插件。

## 验证与记录

整合前重新执行 83 项 Swift CPU 测试，覆盖 HTTP/SSE、调度、阶段接口及 MTP 成本；41 项 Python CPU 测试覆盖进程组清理、HTTP 终态/SSE 解析与路由分析；新增配置命令的 10 项合成 CPU 测试也全部通过。原生 expert32 插件从当前源码重新构建，ABI 2，原始输入未改变，并成功生成绑定实际模型和库的配置。构建脚本同时修正 `native/` 目录大小写，支持与 Git 路径一致的大小写敏感文件系统。构建本身不代表数值或性能验收。

最近的普通 AR/D2 整模型回归属于 `0309521` 对应记录，见[专家共享实验](MTP_GROUPED_GATEUP_EXPERIMENT.md)。各历史实模测试的输入、二进制和覆盖范围保留在对应文档，不把它们合并称为当前版本已重跑的完整服务测试。

本轮 prefill 重测使用完整 11,057-token 输入及 128-token AR 输出；两个独立进程都先 A/B 暖机，随后分别测量 ABBA、BAAB。A 为默认路径，B 为新建配置的 expert32/grouped down；保留原归约。只有输出、阶段边界、路径计数通过，并且两个窗口均至少提升 3%、各窗口两次基线时间漂移不超过 5%，才继续评估默认启用。所有原始样本保留，漂移窗口不以跨窗平均替代。

## 后续阶段收尾

每个实验阶段完成时，将可用代码、复现命令和明确的默认决策及时纳入默认分支。收益稳定的能力进入正常入口；有价值但未验收的能力保持显式选择；失败实验保存简短结论与必要探针。分支保留用于追溯，不能成为唯一的能力或结论存放位置。
