# 独立 runner 转型与带宽验证

2026-09-06，依据用户最新要求。当前主线是独立 Swift runner 的整模型性能；现在推进 native MTP，先补生成会话 / API 的稳定性基础，再开发前缀树与 SSD offload 等状态缓存管理。普通单 token decode 保留为对照，先前“MTP 暂缓”的执行限制已被替代。

GPU 后端和完整 48 层文本生成已落地，入口为 `generate-gpu`；具体用法与验证见 [GPU_RUNNER.md](GPU_RUNNER.md)。`MoEBlockRunner` 继续保留 Core ML 实验；作者 MLX HTTP 服务用于独立对照。

## 可以复用的资产

- `ModelManifest`、`NGramTable`、`NGramHash`：模型配置、SSD 表格式、按需读取和 hash 的已验证语义。
- `ExpertRouter`、真实 fixtures、独立 FP32 参考：用于检查路由、量化解码、分支合并及输出。CPU router 作为参考，不强制成为 GPU 正常运行的数据往返边界。
- `CoreMLBlockRunner`、`ConcurrentExpertKernel` 和当前 MoE 调度：作为 Core ML 后端继续维护；其 FP16 布局、专家包编译和固定容量限制不提升为通用接口约束。
- Swift CLI、错误处理和结果报告：复用操作入口，新增后端或统计字段时明确已实现范围。无需先大范围改目录名或修改现有测试路径。

## 后端边界

优先围绕完整 MoE/block 建立统一执行语义：输入张量、明确的计算精度、模型参数及状态、输出张量、同步完成点和可选诊断信息。GPU 后端内部可在一次批量操作中完成路由、top-k、Q4 专家和加权合并。

通用层不要要求“每个专家必须调用一次 predict”，不要强制补零到32，也不要把每个中间结果转换为 Swift `[Float]`。设备缓冲的所有权与生命周期应贯穿连续 block，主机读取作为诊断或最终输出的显式操作。

prefill 与 decode 使用同一数学语义，但选择不同内核和分块。已有 GDN/attention 连续状态、QSA 阈值及 reset 对照，并已运行完整输出 head 和生成；局部 fixture 的覆盖范围、整模型短请求对照和真实长文档验证分别记录。

## 第一轮固定条件（历史基线）

- 单请求、单序列、普通单 token decode；MTP、drafter、PLD 和前缀缓存关闭。
- 同一原始 Q4/group64 权重、相同采样设置、实际 token 输入与上下文；prefill、稳定 decode、首次载入分别记录。
- 先在现有4096上下文预算内使用真实中文、代码和文档；长输入加输出须跨过实际 QSA 激活条件，同时不超出上下文上限。
- 内核对照先复用真实 layer0 MoE fixtures，再扩大连续状态与层覆盖。每个性能变化同时接受数值检查。

## 必须区分的三类数据

| 口径 | 定义 | 限制 |
|---|---|---|
| 逻辑有效载荷速率 | 实际执行所需参数/状态的定义明确字节数 ÷ 对应阶段耗时 | 这是账本估计，可能被片上缓存复用；不等于 DRAM 物理流量 |
| 实际 DRAM 带宽 | 同一窗口、可归属的硬件读写字节计数增量 ÷ 该窗口耗时 | 明确读/写、GPU/全芯片口径与其他负载；无计数器时填未知，不能反推伪造 |
| 实际 SSD I/O | 同一请求窗口的物理读取量、缺页及等待 | 按需读取的逻辑行字节和 pread 返回量可能来自文件缓存，不能称为SSD实际带宽 |

MoE 账本仅计实际执行的专家，包含 packed Q4、scale/bias；共享专家、主干、head 和状态按实际路径另计。MTP 参数、不执行专家和整个51.2GB表不摊成每个普通 token 的读取量。缓存流量的口径在各轮保持一致，不凭总模型大小乘 token/s 宣称带宽。

物理计数器与耗时必须来自同一测量窗口。不同运行的硬件区间中位数不能相加后从整层时间相减，用来声称精确的框架开销。多专家并发调用耗时之和也不是墙钟时间。

利用率参考本机同类访问模式的可持续上限，保留芯片宣传峰值作背景；不能将 memcpy 的读加写字节直接与只读内核的口径混比。顺序大块读取、真实Q4解码和动态专家访存各自记录。

## 第一项实现与通过条件

MoE/block 后端、原始 Q4 路径、完整路由、载荷账本和时间窗口已实现。融合路由、Q4 decode、HC 与 GDN 已逐项核对；物理 DRAM 字节计数仍不可用，报告保留 null。后续重点是更广泛的真实请求、长上下文性能及能耗，而非继续把局部执行当作整模型支持。

默认 `reference` 模式保留作者的 BF16 prefill 归约和最后一个 prompt token 的 decode 步骤。当前短请求前 16 个 token 与已有作者参考一致；真实长文档已完成 2185 输入 token、8 输出 token 的一次 QSA 执行检查，完整 EOS 作者对照和长文档数值对照待补。原 prefill 局部输出对独立 FP32 的相对 L2 为 0.5014146%，略高于 0.5% 门槛，该项仍为失败；源语义一致性是另一项检查。可选 `float32` prefill 累加改善该局部 FP32 误差，但已观察到生成 token 路径变化，不作为默认。具体原始记录及范围见 [GPU 验证说明](GPU_RUNNER.md)。

优先检查权重重复读取、量化展开临时内存、输出回传、过小dispatch及无效同步，逐项与固定参考比较。优化只有在数值通过、单 token 耗时降低、且流量变化能够解释时才接受。

降低冗余流量、提高缓存复用可能让物理GB/s下降，同时让推理变快；这属于有效优化。因此同时报告耗时、有效载荷和实际流量，不以“DRAM读得越满越好”代替整模型性能。

上述第一轮以 AR 建立基线；现在 MTP 已进入独立开发与验收，不能继续将“MTP 暂缓”作为当前限制。现有本机 cooperative PD、MTP 状态及长输入回归分别以 [PD 说明](docs/PREFILL_DECODE_SEPARATION.md)、[MTP 发布条件](docs/MTP_RELEASE_CRITERIA.md)和各实验记录为准。2026-09-07 新增的 [上游特性吸收计划](docs/UPSTREAM_ADOPTION_PLAN.md)给出后续实施顺序：GDN 载入实验、MTP 完整成本与输出延迟、完整状态前缀复用及 SSD 缓存。
